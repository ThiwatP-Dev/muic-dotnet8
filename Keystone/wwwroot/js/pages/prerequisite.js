$(document).ready( function() {
    var contractTable = new RowAddAble({
        TargetTable: "#js-and-condition",
        TableTitle: 'And Condition',
        ButtonTitle: 'Condition'
    });
    contractTable.RenderButton();

    var contractTable = new RowAddAble({
        TargetTable: "#js-or-condition",
        TableTitle: 'Or Condition',
        ButtonTitle: 'Condition'
    });
    contractTable.RenderButton();

    var contractTable = new RowAddAble({
        TargetTable: "#js-grade-condition",
        TableTitle: 'Grade Condition',
        ButtonTitle: 'Condition'
    });
    contractTable.RenderButton();

    var contractTable = new RowAddAble({
        TargetTable: "#js-course-group-condition",
        TableTitle: 'Course Group Condition',
        ButtonTitle: 'Condition'
    });
    contractTable.RenderButton();

    var contractTable = new RowAddAble({
        TargetTable: "#js-term-condition",
        TableTitle: 'Term Condition',
        ButtonTitle: 'Condition'
    });
    contractTable.RenderButton();

    var contractTable = new RowAddAble({
        TargetTable: "#js-gpa-condition",
        TableTitle: 'GPA Condition',
        ButtonTitle: 'Condition'
    });
    contractTable.RenderButton();

    var contractTable = new RowAddAble({
        TargetTable: "#js-credit-condition",
        TableTitle: 'Credit Condition',
        ButtonTitle: 'Condition'
    });
    contractTable.RenderButton();

    var contractTable = new RowAddAble({
        TargetTable: "#js-total-course-group",
        TableTitle: 'Total Course Group Condition',
        ButtonTitle: 'Condition'
    });
    contractTable.RenderButton();

    var contractTable = new RowAddAble({
        TargetTable: "#js-batch-condition",
        TableTitle: 'Batch Condition',
        ButtonTitle: 'Condition'
    });
    contractTable.RenderButton();

    var contractTable = new RowAddAble({
        TargetTable: "#js-ability-condition",
        TableTitle: 'Ability Condition',
        ButtonTitle: 'Condition'
    });
    contractTable.RenderButton();
});