@model PredefinedCourse
@{
    ViewData["Title"] = "Add Predefined Course"; 
}

<section class="row">
    <div class="col-12">
        <div class="block block--underline">
            <form class="ks-form container" asp-controller="PredefinedCourse" asp-action="Create" method="POST">
                @Html.AntiForgeryToken()
                <partial name="_Form"/>
            </form>
        </div>
    </div>
</section>